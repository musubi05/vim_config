" ビジュアルモードの選択色カスタム
autocmd ColorScheme * highlight Visual term=reverse cterm=reverse ctermbg=240 guibg=#454a60
" カラースキーム
colorscheme iceberg
" 透過
set transparency=6
" Visual選択で選択されたテキストが、自動的にクリップボードレジスタ "*" にコピーされる。
set guioptions+=a
